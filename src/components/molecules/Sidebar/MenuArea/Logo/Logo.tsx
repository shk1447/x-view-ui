import { styled } from '@mui/material/styles';
import _SvgIcon from '@mui/material/SvgIcon';
import { IconProps } from '../interfaces';

const Logo = styled(_SvgIcon)<IconProps>(({ theme }) => ({}));

export default Logo;
